plugins {
    // Lets Gradle download a matching JDK automatically when the toolchain is missing locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "DeACoudre"
